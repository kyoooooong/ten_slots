# Architecture Decision Records

설계 과정에서 실제로 고민한 쟁점과 판단 근거를 기록합니다. 선택한 것뿐 아니라 검토했지만 채택하지 않은 방향도 함께 남겨두었습니다. "무엇을 골랐는가"보다 "왜 이 선택이 이 시스템에 맞는다고 보았는가"에 초점을 두었습니다.

---

## 1. 아키텍처 — 헥사고날

이 시스템에는 런타임에 구현체가 교체되어야 하는 지점이 두 군데 있습니다. Redis 장애 시 재고 차감 어댑터가 `RedisStockAdapter`에서 `DbStockAdapter`로 전환되어야 하고, 결제 수단은 `CREDIT_CARD`, `Y_PAY`, `POINT` 세 가지가 동일한 처리 흐름에 플러그인 형태로 연결되어야 합니다. 이 두 요구사항을 얼마나 자연스럽게 수용할 수 있는지를 기준으로 아키텍처를 골랐습니다.

레이어드 아키텍처도 인터페이스를 뽑으면 의존성을 역전시킬 수 있습니다. 하지만 이것은 개발자 규율에 의존하는 것이지, 구조가 강제하는 것이 아닙니다. 의존성 방향이 명시적으로 강제되지 않으면 레이어 경계가 점진적으로 흐려질 위험이 있고, 특히 Redis Fallback처럼 구현체 교체가 빈번한 경우 서비스 레이어에 분기 로직이 쌓이기 쉽습니다.

헥사고날은 의존성 방향을 컴파일 시점에 강제합니다. `BookingService`는 `StockPort`와 `PaymentGatewayPort`라는 인터페이스만 바라보고, 그 뒤에 Redis가 있는지 DB가 있는지, 어떤 PG사인지는 Adapter가 결정합니다. `CircuitBreakerStockAdapter`가 `@Primary`로 `StockPort`를 구현하면서 내부에서 Redis → DB로 라우팅하고, `BookingService`는 이 사실을 알지 못합니다. 새 결제 수단이 추가될 때도 `PaymentGatewayPort` 구현체 하나만 만들면 되고, `BookingService`와 `CompositePaymentProcessor`에는 코드 변경이 없습니다.

```
BookingService
  ├── StockPort ← CircuitBreakerStockAdapter (@Primary)
  │                └─ RedisStockAdapter (정상 시)
  │                └─ DbStockAdapter   (CB OPEN / 키 없음)
  └── PaymentProcessorPort ← PaymentGatewayRouter
                              └─ CreditCardGatewayAdapter
                              └─ YPayGatewayAdapter
                              └─ PointGatewayAdapter
```

멀티모듈도 검토하였습니다. 하지만 멀티모듈은 팀이 나뉘거나 배포 단위가 달라질 때 의미가 있는 구조입니다. 이 프로젝트는 단일 Spring Boot 애플리케이션이고, 모듈을 나눠서 얻는 이점보다 설정 복잡도와 빌드 비용이 더 크다고 판단하였습니다. 대신 패키지 경계를 헥사고날 레이어에 맞춰 명확히 나눔으로써 모듈 분리와 비슷한 효과를 얻고자 하였습니다.

이 프로젝트 규모에서 헥사고날이 과도한 구조라는 시각도 있을 수 있습니다. 그럼에도 이 선택을 한 것은, "Redis 장애 시 투명한 fallback"과 "결제 수단 확장"이 이 시스템의 핵심 요구사항이고, 헥사고날이 그 요구를 구조 자체로 표현하는 데 가장 적합하다고 보았기 때문입니다.

---

## 2. 재고 차감 동시성 — Redis Lua + Circuit Breaker + DB Fallback

00시에 수백 개의 요청이 동시에 재고 차감을 시도하는 상황입니다. 이 지점에서 초과 판매가 발생하면 복구 비용이 크기 때문에, 재고 차감 전략을 결정하는 데 가장 많은 시간을 들였습니다.

**DB 비관적 락 (SELECT FOR UPDATE)**: 구현이 단순하고 정합성을 보장합니다. 그러나 lock 점유 시간이 트랜잭션 전체로 늘어납니다. 이후 단계인 PG 호출(최대 10초)을 트랜잭션 안에 넣으면 row lock이 그 시간 동안 유지되고, 트랜잭션 밖으로 빼더라도 DB 레벨에서 직렬화가 발생해 처리량이 크게 제한됩니다. 00시 spike를 처리하기에는 병목이 지나치다고 판단하였습니다.

**DB 낙관적 락 (@Version)**: 비관적 락보다 처리량이 높고 구현도 비교적 단순합니다. 그러나 충돌이 잦으면 재시도 폭풍(retry storm)이 발생합니다. 요청 수가 슬롯 수(10개)보다 압도적으로 많은 00시 상황에서는 대부분의 요청이 충돌로 실패하고, 재시도가 다시 충돌을 유발하는 악순환이 생깁니다. 낙관적 락은 충돌이 드문 상황에서 이점을 발휘하는데, 이 시스템의 핵심 구간이 그 반대에 해당합니다.

**Redis Lua 스크립트**: Redis는 단일 스레드로 Lua 스크립트를 원자적으로 실행합니다. EXISTS → DECR → 음수 검증을 하나의 명령처럼 처리하므로, 어떤 요청도 이 블록 사이에 끼어들 수 없습니다. 10개 슬롯이 소진되면 이후 요청은 즉시 `-1`을 받고 돌아갑니다. DB로 향하는 요청 수 자체가 슬롯 수 수준으로 줄어드는 것이 핵심입니다.

Lua 스크립트의 반환값을 세 가지로 구분한 것이 이 설계에서 중요한 부분입니다. `-2`는 키가 아예 없는 경우(warm-up 미실행), `-1`은 재고 소진, `0 이상`은 성공입니다. `-2`는 Redis 인프라 문제가 아니라 키 부재이므로 Circuit Breaker failure로 집계하지 않고 DB로 라우팅합니다. 이를 구분하지 않으면 warm-up이 조금 늦었다는 이유만으로 CB가 OPEN되어, 정상적인 Redis를 사용할 수 없게 되는 문제가 생깁니다.

Circuit Breaker는 Redis 자체가 불안정할 때를 위한 방어선입니다. 실패율이 임계치를 넘으면 CB가 OPEN되고, 이후 요청은 Redis 시도 없이 즉시 DB로 향합니다. Redis가 불안정한 구간에서 매 요청마다 타임아웃을 기다리는 대신, CB OPEN 상태에서는 즉시 fallback하여 지연 누적을 방지합니다.

```
요청 → CircuitBreakerStockAdapter
        ├─ CB OPEN?  → DbStockAdapter (즉시)
        └─ CB CLOSED → RedisStockAdapter
                        ├─ Lua 반환 -2 (키 없음) → DB (CB failure 미집계)
                        ├─ Lua 반환 -1 (소진)    → STOCK_SOLD_OUT
                        ├─ Lua 반환 ≥0 (성공)    → 통과
                        └─ 예외 (Redis 장애)      → DB (CB failure 집계)
```

---

## 3. DB Fallback 잠금 전략 — 낙관적 락

DB Fallback이 동작하는 시점, 즉 Redis가 불안정할 때도 재고 정합성은 보장되어야 합니다. 이 구간에서 비관적 락과 낙관적 락 중 무엇이 적합한지를 별도로 검토하였습니다.

비관적 락은 재고 row를 한 번에 하나의 트랜잭션만 처리하도록 직렬화합니다. 정확하지만, CB가 막 OPEN된 순간 Redis를 포기하고 DB로 향하는 요청들이 동시에 몰릴 때 모두 같은 row의 lock을 기다리며 DB 커넥션을 점유합니다. lock 경합이 심해지면 타임아웃이 발생하고, 최악의 경우 데드락으로 이어질 수 있습니다.

낙관적 락은 `@Version` 필드로 구현합니다. 모든 요청이 lock 없이 진행되다가 UPDATE 시점에 version을 비교하고, 충돌한 요청만 재시도합니다. 전체적으로는 직렬화 없이 병렬 처리되다가 커밋 단계에서만 충돌을 감지하는 구조입니다.

처음에는 Fallback 상황에서도 트래픽이 많을 수 있다는 이유로 비관적 락을 고려하였습니다. 그러나 Fallback에 진입하는 시점에는 이미 Nginx rate limiting과 Circuit Breaker를 통과한 요청만 남아 있어, 실질적인 동시 요청 수가 대폭 감소한 상태입니다. 이 구간에서 충돌 확률이 낮다면, 커넥션 점유 시간이 짧은 낙관적 락이 비관적 락보다 낫다고 판단하였습니다. `MAX_RETRY=3`으로 재시도를 제한하고, 소진 시 `LOCK_CONFLICT(503)`를 반환하여 retry storm을 차단하였습니다.

`StockDecreaseHelper`에 `@Transactional(propagation = REQUIRES_NEW)`를 적용해 매 시도마다 새 트랜잭션을 시작합니다. 같은 트랜잭션 안에서 재시도하면 이미 version 충돌로 실패한 트랜잭션을 재사용하게 되어 재시도가 의미를 잃습니다.

---

## 4. 트랜잭션 경계 — PG 호출을 트랜잭션 밖으로

`BookingService.book()`에 `@Transactional`을 걸고 모든 단계를 하나의 트랜잭션으로 묶는 방식을 처음에 고려하였습니다. 단순하고 롤백이 자동이라는 장점이 있습니다. 그러나 PG 응답 시간을 고려하면 이 방식은 현실적으로 동작하지 않습니다.

PG 호출은 네트워크를 타는 외부 I/O입니다. 평균 1~2초이고, 최대 timeout은 10초로 설정하였습니다. `@Transactional` 안에 PG 호출이 들어가면 트랜잭션이 시작된 시점부터 PG 응답이 올 때까지 DB 커넥션이 점유됩니다. 커넥션 풀이 20개라면, 20명이 동시에 PG 응답을 기다리는 순간 커넥션이 고갈됩니다. 21번째 요청부터는 커넥션 획득 자체에서 타임아웃이 발생합니다. 00시 spike에서 이 상황은 높은 확률로 발생합니다.

해결책은 PG 호출을 트랜잭션 밖으로 꺼내고, DB 조작은 필요한 시점에만 짧게 독립 트랜잭션(`REQUIRES_NEW`)으로 처리하는 것입니다. `BookingService`에 `@Transactional`을 선언하지 않았습니다.

```
[TX 없음        ] 멱등키 획득 (Redis)
[TX 없음        ] 상품 조회 + 검증
[TX 없음        ] 재고 차감 (Redis)
[TX REQUIRES_NEW] PENDING 예약 저장   → 즉시 커밋
[TX REQUIRES_NEW] REQUESTED 결제 저장 → 즉시 커밋
[TX 없음        ] PG 호출  ← 커넥션 미점유
[TX REQUIRES_NEW] APPROVED 결제 저장  → 즉시 커밋
[TX REQUIRED    ] booking CONFIRMED + payment CONFIRMED → 단일 원자 커밋
```

각 단계를 즉시 커밋하면 서버가 중간에 죽더라도 어느 단계까지 완료됐는지 DB 레코드만으로 파악할 수 있습니다. PENDING 레코드와 `pgIdempotencyKey`가 복구 기준점이 됩니다. 전체를 하나의 트랜잭션으로 묶었다면 서버 장애 시 모든 변경이 롤백되어 이 기준점 자체가 사라집니다.

이 방식의 트레이드오프는 명확합니다. 롤백이 자동이 아니기 때문에, 실패 시 보상 처리를 직접 구현해야 합니다. 재고 복구(`increase`), 멱등키 해제(`release`), 예약 FAILED 처리를 각 catch 블록에서 직접 처리하였습니다. 이 보상 로직 자체가 실패할 가능성도 있어, 로그를 남기고 수동 복구 경로를 열어두었습니다. 완전한 자동화보다 "언제 무엇이 실패했는지 추적 가능하게 만드는 것"이 현실적인 목표라고 생각하였습니다.

덧붙여, PG 호출을 트랜잭션 안에 두면 단순한 커넥션 점유 문제에 그치지 않습니다. 트래픽이 몰리는 순간 여러 요청이 동시에 커넥션을 잡은 채 PG 응답을 기다리면, 커넥션 풀이 고갈되면서 데드락으로 이어질 수 있습니다. 결제 시스템을 운영하다 보면 이 패턴에서 기인한 장애가 드물지 않게 발생하며, 이 점이 트랜잭션 경계를 신중하게 설계해야 한다는 판단을 더욱 확고하게 해주었습니다.

---

## 5. 복합 결제 원자성 — 동기 롤백

카드 + 포인트처럼 결제 수단을 복합으로 사용할 때, 카드는 승인됐는데 포인트 차감이 실패하면 어떻게 처리해야 하는지가 문제입니다. 이미 승인된 결제를 그대로 두면 돈만 빠지고 예약이 없는 상태가 됩니다.

**비동기 보상 트랜잭션 (Saga 패턴)**: 각 결제 수단을 독립 트랜잭션으로 처리하고, 실패 시 이미 완료된 단계에 대한 보상 이벤트를 발행합니다. 시스템이 분산되어 있거나 다수의 downstream 서비스가 연결된 경우에는 이 방식이 적합합니다. 하지만 보상 이벤트를 소비하는 Consumer가 별도로 필요하고, Consumer 자체가 실패하면 또 다른 재처리 메커니즘이 필요합니다. 이 시스템에서 복합 결제는 하나의 요청 흐름 안에서 동기적으로 처리되고 있어, 이 복잡도를 감수할 근거가 없다고 판단하였습니다.

**동기 취소 (현재 구현)**: 복합 결제 중 하나가 실패하면, 그 이전에 승인된 결제들을 즉시 PG에 취소 요청합니다. `CompositePaymentProcessor`의 `cancelApproved()`가 승인된 결제 목록을 순회하면서 PG 취소 API를 호출하고, 각 건을 DB에 FAILED로 기록합니다.

취소 API 자체가 실패하는 경우도 고려하였습니다. 이 경우 PG에서는 승인됐는데 내부 DB에는 FAILED로 기록되는 불일치가 생깁니다. 이 상황은 자동으로 해결할 수 없습니다. `pgTransactionId`를 DB에 보관하고 있으므로, 수동 대사로 복구하는 경로를 남겨두었습니다.

결제 순서에도 의도를 담았습니다. 포인트는 자체 관리 포인트라 취소 처리가 확실한 반면, 외부 PG(카드, Y페이)는 취소 실패 가능성이 있습니다. 복합 결제 시 포인트를 먼저 처리하고 외부 PG를 나중에 두면, 외부 PG가 실패했을 때 취소해야 할 대상이 포인트뿐이어서 롤백이 안전해집니다. 반대 순서로 외부 PG를 먼저 승인하고 포인트가 실패하면, 이미 나간 외부 PG 거래를 취소해야 하는데 이 취소가 또 실패할 수 있어 상황이 복잡해집니다.

---

## 6. PG 멱등성 — timeout 후 inquiry

PG 호출에서 timeout이 발생했을 때, 두 가지 상황이 가능합니다. PG 서버가 요청을 처리했는데 응답만 늦게 온 것인지, 아니면 처리 자체가 안 된 것인지입니다. 이를 구분하지 않고 실패로 처리하거나 단순 재시도하면 이중 청구 또는 미청구 문제가 생깁니다.

`bookingId:결제수단` 조합을 `pgIdempotencyKey`로 만들어 PG에 전달합니다. PG 호출 전에 이 키를 DB에 `REQUESTED` 상태로 먼저 커밋합니다. timeout이 발생하면 같은 키로 PG에 inquiry 요청을 보내 실제 처리 여부를 확인한 뒤 상태를 확정합니다.

```
PG 호출 → PaymentTimeoutException
         → inquiry(pgIdempotencyKey)
         → 승인됐으면 approve, 아니면 fail
```

이 키는 서버가 재시작된 이후에도 유효합니다. DB에 남아있는 `REQUESTED` 레코드와 `pgIdempotencyKey`를 이용해 PG에 조회하면 상태를 복구할 수 있습니다. 단, inquiry 자체가 실패하는 경우(PG 서버 전체 장애)는 자동 복구가 불가능합니다. 이 경우도 DB 레코드를 기준으로 수동 대사로 처리하는 것이 현실적인 방향이라고 생각하였습니다.

`bookingId`는 TSID로 생성해 요청 진입 시점에 할당합니다. PG 호출 전에 이미 ID가 확정되어 있어 `pgIdempotencyKey`를 DB 저장 → PG 호출 순서로 처리할 수 있습니다. DB auto-increment 방식이었다면 저장 후에야 ID를 알 수 있어 이 순서가 복잡해졌을 것입니다.

실서비스 전환 시에는 PG사와의 통신에 전용선 또는 VPN 적용이 필요합니다. 현재 구현에서는 Mock PG를 사용하고 있으나, `PaymentGatewayPort` 인터페이스를 교체하는 것만으로 실제 PG사 어댑터로 전환할 수 있습니다.

---

## 7. API 멱등성 — Redis SET NX

클라이언트가 버튼을 두 번 누르거나, 네트워크 오류로 요청이 재전송되면 동일한 예약 요청이 중복으로 들어올 수 있습니다. 이를 처리하는 방법으로 DB unique key 제약과 Redis SET NX를 비교하였습니다.

DB unique key는 두 요청이 모두 재고 차감까지 진행한 뒤 DB INSERT 시점에 하나가 실패합니다. 이미 차감한 재고를 복구해야 하고, 두 요청이 동시에 재고를 차감하려 시도했다면 재고 정합성 문제가 발생할 수 있습니다.

Redis SET NX는 첫 번째 요청이 키를 선점하는 순간 두 번째 요청을 즉시 차단합니다. DB까지 내려가지 않습니다. 처리 성공 시 키를 유지해 동일 키로 재예약을 막고, 실패 시 키를 해제해 클라이언트 재시도를 허용합니다.

TTL을 24시간으로 설정한 근거를 남겨두고 싶습니다. 단순 중복 차단만이 목적이라면 수십 초면 충분합니다. 그러나 이 시스템에서 예약 성공은 "오늘 이 상품 1건"을 의미합니다. 키가 유지되는 동안 같은 `idempotencyKey`로 다시 예약을 시도할 수 없고, 24시간 후 자동 만료되어 다음 날 오픈에 영향을 주지 않습니다. 서버 장애로 키가 해제되지 못한 경우에도 자정 이후 자연스럽게 만료됩니다. 다만 이는 초기 요구사항의 범위를 넘어선 선택이므로, 프로덕트 정책에 따라 TTL을 조정할 수 있습니다.

`idempotencyKey`는 클라이언트가 생성해 요청에 포함합니다. 서버에서 생성하면 클라이언트 재시도 시 다른 키로 요청하게 되어 멱등성이 보장되지 않습니다.

---

## 8. 00시 트래픽 대응 — Nginx rate limiting + Redis warm-up

자정 오픈 직후 수백~수천 개의 요청이 동시에 들어오는 상황입니다. 이 spike를 처리하는 전략을 두 층으로 나누었습니다.

**첫 번째 층: Nginx rate limiting**

IP당 초당 10req, burst 30으로 설정하였습니다. burst는 순간 폭발을 일정 범위까지 허용하며, `nodelay`를 붙여 burst 범위 내 요청은 즉시 처리합니다. 그 이상은 `503`으로 즉시 반환하여 백엔드를 보호합니다.

이 구간에서 Redis ZADD를 이용한 공정성 큐도 검토하였습니다. 타임스탬프 기준으로 진입 순서를 먼저 확정하고, 그 순서대로 처리하면 서버 두 대 환경에서 어느 서버에 붙느냐로 유불리가 생기는 것을 방지할 수 있습니다. 다만 큐잉 구조를 도입하면 예약 요청이 비동기화되어 "접수 완료, 처리 중" 응답 후 결과를 별도로 전달해야 합니다. 이 시스템은 예약과 결제 결과를 단일 요청에서 즉시 응답하는 구조이므로, 큐잉 방식은 응답 구조를 근본적으로 바꿔야 한다는 점에서 채택하지 않았습니다. 대신 Nginx rate limiting으로 진입 자체를 제어하고, Redis 재고 차감의 원자적 처리로 정합성을 보장하는 방향을 선택하였습니다.

checkout API는 30r/s로 여유 있게 설정하였습니다. 00시 직전 새로고침이 집중되는 패턴을 수용하면서, booking보다 처리 비용이 낮다는 점을 반영하였습니다.

**두 번째 층: Redis warm-up**

재고를 DB에서 직접 차감하면 모든 요청이 DB lock 경합을 합니다. Redis에 재고를 사전 적재해두면 대부분의 요청이 Lua 스크립트 한 번으로 처리됩니다. DB로 가는 요청 수가 슬롯 수(10개) 수준으로 줄어듭니다.

warm-up 스케줄러는 매분 실행하며 오픈 10분 이내 상품을 Redis에 적재합니다. "오픈 직전 1회 실행" 방식도 고려하였지만, 두 가지 이유로 슬라이딩 윈도우 방식을 선택하였습니다. 첫째, Redis 장애 후 재기동 시 다음 분에 자동으로 재적재됩니다. 별도 운영 개입 없이 복구됩니다. 둘째, `setIfAbsent`를 사용하므로 판매 중 스케줄러가 재실행되더라도 진행 중인 재고를 덮어쓰지 않습니다.

warm-up이 실패하면 해당 상품은 `StockKeyNotFoundException(-2)`을 받아 DB fallback으로 처리됩니다. Redis 인프라 문제와 구분되므로 Circuit Breaker failure로 집계되지 않습니다.

---

## 9. 분산 환경 ID — TSID

앱 서버가 2대이므로 ID 생성 전략이 필요합니다. 선착순 예약 시스템에서 ID가 시간 순으로 정렬 가능하면 "몇 번째 예약인지" 조회가 자연스럽고, 생성 시점 추적도 용이합니다.

**DB auto-increment**: 가장 단순합니다. 하지만 INSERT를 해야 ID가 생깁니다. `pgIdempotencyKey`를 PG 호출 전에 DB에 저장하려면 bookingId가 먼저 확정되어 있어야 하는데, auto-increment는 저장 후에야 ID를 알 수 있습니다. DB 단일 시퀀스 병목도 존재합니다.

**UUID v4**: DB 없이 생성 가능하고 충돌 확률이 사실상 0입니다. 그러나 랜덤이라 시간 순 정렬이 되지 않고, 36자 문자열로 인덱스 효율이 떨어집니다.

**UUID v7**: 시간 순 정렬이 가능하고 분산 환경에서도 동작합니다. 그러나 36자 문자열이고, Java 표준 라이브러리에 포함되지 않아 외부 의존성이 필요합니다.

**TSID**: 타임스탬프 + 노드 ID + 시퀀스를 조합한 64비트 정수입니다. Base32로 인코딩하면 13자 문자열이 됩니다. UUID v7보다 짧고, 노드 ID가 포함되어 분산 환경에서 충돌이 없습니다. 시간 순 정렬이 가능하고, DB INSERT 없이 애플리케이션에서 즉시 생성됩니다.

노드 ID는 Docker Compose에서 인스턴스별로 `-Dtsid.node=1`, `-Dtsid.node=2`로 주입하였습니다. 인스턴스가 늘어나면 새 노드 ID를 부여하면 되고, 코드 변경은 없습니다.

엔티티별 ID 전략도 구분하였습니다. `Booking`, `Payment`, `Product`는 TSID를 사용하였고, `User`는 외부에서 관리되는 마스터 데이터라 IDENTITY(Long)을 유지하였습니다. `Stock`은 `Product`와 1:1이므로 `productId`가 자연키입니다. 별도 식별자가 필요하지 않습니다.

---

## 10. 모니터링 — Prometheus + Grafana + Micrometer

결제 시스템에서 장애가 발생했을 때 가장 먼저 드는 의문은 "어느 단계에서 문제가 생겼는가"입니다. 재고 차감에서 실패한 건지, PG 승인에서 막힌 건지, 아니면 DB 확정 단계에서 터진 건지를 빠르게 파악하지 못하면 대응이 늦어집니다. 이 문제를 단순 로그로만 추적하는 데는 한계가 있어, 핵심 흐름 지점마다 Micrometer 카운터를 심고 Prometheus로 수집해 Grafana에서 시각화하는 구조를 채택하였습니다.

Datadog 같은 상용 APM도 검토하였습니다. 결제 단계별 실시간 모니터링에 특화되어 있고 가시성이 높다는 장점이 있습니다. 그러나 이 프로젝트는 Docker Compose로 전체 스택을 자체 운영하는 구조이고, 외부 SaaS에 의존하는 것보다 인프라와 함께 띄울 수 있는 Prometheus + Grafana가 더 자연스럽다고 판단하였습니다. 실서비스 전환 시에는 트래픽 규모와 팀 운영 여건에 따라 상용 APM 도입을 재검토할 여지가 있습니다.

코드에 심은 카운터는 다섯 가지입니다. 각각의 의미를 명확히 정의하고 추가하였습니다.

| 메트릭 | 의미 |
|--------|------|
| `booking.confirmed` | 예약 최종 확정 수 — 핵심 비즈니스 지표 |
| `booking.stock.soldout` | 재고 소진으로 인한 거절 수 — 슬롯 소진 시점 확인 |
| `booking.duplicate` | 중복 요청 차단 수 — 클라이언트 재시도 패턴 감지 |
| `payment.result` (success / fail) | 결제 성공·실패 수 — PG 이상 징후 조기 감지 |
| `stock.redis.fallback` | Redis → DB Fallback 전환 수 — Circuit Breaker 동작 여부 |

`booking.confirmed`와 `payment.result(success)`를 나란히 보면 결제는 됐는데 예약 확정이 안 된 케이스를 잡을 수 있습니다. `stock.redis.fallback`이 급격히 올라가면 Redis 인프라에 이상이 생긴 것으로 판단하고 선제 대응할 수 있습니다.

Resilience4j Circuit Breaker는 Micrometer와 자동으로 연동되어 CB 상태 전환(CLOSED → OPEN → HALF_OPEN) 메트릭도 별도 구성 없이 수집됩니다. `stockDecrease` CB 인스턴스의 상태 변화를 Grafana에서 함께 시각화하면 재고 차감 경로의 전체 흐름을 한눈에 파악할 수 있습니다.

---

## 11. 국제화 — UTC 중심 시간 설계

이 시스템은 한국 서비스로 시작하지만, 해외 사용자나 글로벌 운영 환경을 처음부터 고려하였습니다. 시간 처리에서 가장 흔한 실수는 "지금은 한국 서비스니까 KST로 저장해도 된다"는 판단입니다. KST로 저장하면 나중에 서머타임이 없는 한국에서는 문제가 없더라도, 해외 PG사 연동, 글로벌 CDN 로그 분석, 다국 배포 시 시간대가 맞지 않는 장애가 발생합니다.

**모든 시각은 UTC로 저장합니다.**

Hibernate `hibernate.jdbc.time_zone: UTC`를 설정해 JDBC 레벨에서 UTC로 변환하여 MySQL에 저장합니다. MySQL `DATETIME` 컬럼은 시간대 정보를 저장하지 않으므로, 애플리케이션이 일관된 기준으로 읽고 쓰는 것이 중요합니다. 이 설정 덕분에 DB에 저장된 값은 항상 UTC이고, 어느 인스턴스에서 읽어도 동일한 값을 얻습니다.

**도메인 전체에서 `OffsetDateTime`을 사용합니다.**

`LocalDateTime`은 시간대 정보를 갖지 않습니다. 애플리케이션 서버가 어느 시간대에 있느냐에 따라 같은 숫자가 다른 절대 시각을 가리킬 수 있습니다. `OffsetDateTime`은 `+00:00`(UTC) 또는 `+09:00`(KST) 같은 오프셋을 값 안에 포함하므로, 어느 환경에서 실행되더라도 절대 시각이 명확합니다. 도메인 객체, API 응답, JPA Auditing(`@CreatedDate`, `@LastModifiedDate`) 모두 `OffsetDateTime`을 사용합니다.

**`Clock`을 Bean으로 주입합니다.**

`OffsetDateTime.now()` 또는 `LocalDateTime.now()`를 직접 호출하면 JVM 기본 시간대의 영향을 받습니다. 대신 `Clock.systemUTC()`를 `ClockConfig`에서 Bean으로 등록하고, 시각이 필요한 곳마다 주입합니다. 이렇게 하면 두 가지 이점이 있습니다. 첫째, 서버가 어느 시간대에 배포되든 UTC 기준이 코드 레벨에서 보장됩니다. 둘째, 테스트 시 `Clock.fixed(...)`로 교체하면 시각에 의존하는 로직을 재현 가능하게 검증할 수 있습니다.

**API 응답은 UTC ISO 8601 (`Z` suffix)로 반환합니다.**

`checkInTime`, `checkOutTime`, `openAt`, `confirmedAt` 등 모든 시각은 `2026-05-10T15:00:00Z` 형식으로 응답합니다. 클라이언트(웹 프론트엔드, 모바일 앱)가 UTC를 받아 사용자의 로컬 시간대로 변환하는 책임을 집니다. 서버가 특정 지역 시간대로 응답하면, 클라이언트는 서버가 어느 기준으로 변환했는지 알아야 하고 이중 변환 오류가 발생합니다. UTC 응답은 이 모호함을 제거합니다.

**TSID는 UTC 기준 타임스탬프를 내장합니다.**

TSID의 상위 42비트는 에포크(2020-01-01T00:00:00Z 기준) 이후 밀리초 단위 Unix 타임스탬프입니다. 이 값이 UTC 기준이므로, 서버 인스턴스의 시간대와 무관하게 TSID 정렬이 항상 시간 순서를 보장합니다. 멀티 리전 배포 시에도 각 인스턴스가 UTC 기준 타임스탬프를 사용하는 한 전역 단조 증가 순서가 유지됩니다.

**JPA Auditing `DateTimeProvider`를 명시적으로 등록합니다.**

Spring Data JPA의 기본 `AuditingHandler`는 `LocalDateTime.now()`를 반환합니다. `BaseTimeEntity`의 `@CreatedDate`, `@LastModifiedDate` 필드가 `OffsetDateTime`이면 타입 불일치로 `IllegalArgumentException`이 발생합니다. `JpaAuditingConfig`에 `DateTimeProvider`를 `OffsetDateTime.now(ZoneOffset.UTC)` 반환으로 등록하고 `@EnableJpaAuditing(dateTimeProviderRef = ...)`에 연결하여 이를 해결하였습니다.

---

## 12. Kafka 미도입

00시 대량 트래픽을 처리하는 방법으로 메시지 브로커를 통한 큐잉을 검토하였습니다. Kafka를 사이에 두면 요청을 브로커에 넣고 즉시 "접수 완료"를 반환한 뒤, Consumer가 실제 재고 차감과 결제를 처리하는 구조입니다.

그러나 이 방식에는 이 시스템에서 받아들이기 어려운 트레이드오프가 있습니다. 클라이언트가 "접수 완료"를 받은 뒤 실제 예약 결과를 알려면 폴링 API를 따로 만들거나 SSE/WebSocket으로 결과를 push해야 합니다. 재고가 없어서 실패했는지, 결제가 실패했는지를 Consumer가 처리한 이후에야 알 수 있습니다. 구현 복잡도가 크게 올라가는데, 이를 감수할 만한 이득이 현재 구조에서는 없다고 판단하였습니다.

또한 Kafka를 사용하면 결제 실패 시 재고를 복구하는 보상 트랜잭션을 별도 토픽으로 발행하고 처리해야 하고, 그 보상 처리도 또 실패할 수 있습니다. 현재 구조에서는 결제 실패 시 동기 흐름 안에서 즉시 재고를 복구하고 예약을 FAILED로 처리하면 끝이지만, Kafka를 사용하면 이 흐름 전체가 분산 트랜잭션 문제로 바뀝니다.

Kafka가 의미 있는 것은 예약 확정 이후 알림 발송, 정산 처리, 쿠폰 지급처럼 여러 downstream 시스템에 이벤트를 fanout해야 할 때입니다. 현재 시스템은 예약하고 결제하면 끝이라 해당 사항이 없고, Redis의 원자적 연산으로 정합성을 충분히 보장할 수 있습니다. 향후 downstream이 붙게 되면 그때 도입을 재검토할 예정입니다.

